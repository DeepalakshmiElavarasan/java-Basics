class LargestElement {
    public static void main(String[] args) {
        int[] arr = {4, 8, 1, 3, 10};
        int max = arr[0];

        for (int num : arr) {
            if (num > max) max = num;
        }

        System.out.println("Largest: " + max);
    }
}
